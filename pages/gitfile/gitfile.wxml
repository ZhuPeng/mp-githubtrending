<view class="page">
<view class="page__hd">
</view>
<view class="page__bd">
  <view class="padding-top" wx:if="{{spinning}}"><wux-spin nested spinning="{{ spinning }}" tip="Loading ..."></wux-spin></view>
  <md md="{{content}}" owner="{{owner}}" repo="{{repo}}"></md>
</view>
</view>